# Data Model

Not applicable. This feature introduces no CatWorld domain entities,
persistence model, API payloads, schema changes, browser storage, external
contracts, or structured application data.

The coordinator orchestration artifact is repository workflow state documented
in Markdown. Its required sections and factual state rules are specified in
`contracts/sidecar-coordinator-artifact.md`.
