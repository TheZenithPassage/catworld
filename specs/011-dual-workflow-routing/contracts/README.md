# Contracts

Not applicable. This feature changes repository workflow documentation only.

It introduces no public API, UI contract, command schema, parser grammar, data payload, persistence schema, browser storage contract, or external integration contract.
