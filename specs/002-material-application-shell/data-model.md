# Data Model

Not applicable. This feature introduces no domain entities, persistence model,
API payloads, schema changes, browser storage, external contracts, or structured
feature data.

The implementation is limited to frontend shell and shared UI state
presentation while preserving existing backend, persistence, API, route, guard,
and domain workflow contracts.
