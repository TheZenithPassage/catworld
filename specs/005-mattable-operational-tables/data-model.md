# Data Model

Not applicable. This feature introduces no domain entities, persistence model,
API payloads, schema changes, browser storage, external contracts or structured
feature data.

The migrated owner, cat, vet, stay, and account-management tables continue to
render existing frontend models returned by existing services.
